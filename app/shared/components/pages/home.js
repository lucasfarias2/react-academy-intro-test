const React = require('react');
const Layout = require('../commons/layout');

const Home = () => (
  <div>
    <Layout />
    <h2>Home page</h2>
    <p>Esta es la vista de la home</p>
  </div>
);

module.exports = Home;
